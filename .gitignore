# API Key
.env
.env.*

# Python
__pycache__/
*.pyc

# Virtual Environment
.venv/
venv/

# Logs
logs/

# 資料庫只在本機／伺服器工作時用。Vercel 上的 /api/ask 改讀 data/rag.jsonl
#（由 export_static_data.export_rag() 產生），不再把整顆 DB 塞進 git——
# SQLite 檔 delta 壓縮差，每天全量 commit 會讓 .git 一直腫。
*.db